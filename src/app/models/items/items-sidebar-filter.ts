
export interface ItemsSidebarFilter {
    //this interface has all the filters from the sidebar
    minPrice?: number;
    maxPrice?: number;
    forFree?: boolean;
    searchText?: string;
    category?: string;
}